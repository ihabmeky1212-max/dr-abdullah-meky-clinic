import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Dr. Abdullah Meky Clinic | Modern Healthcare",
    template: "%s | Dr. Abdullah Meky Clinic",
  },
  description:
    "Professional clinic management and healthcare services by Dr. Abdullah Meky. Book appointments, manage patients, and access quality medical care.",
  keywords: ["clinic", "healthcare", "doctor", "appointments", "medical"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} min-h-screen antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
